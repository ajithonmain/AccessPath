{
  "name": "@accesspath/wordpress",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "WordPress plugin that adds the AccessPath accessibility widget to any site, configured from a settings page.",
  "scripts": {
    "sync-embed": "node scripts/sync-embed.mjs",
    "build": "npm run sync-embed && node scripts/build-zip.mjs",
    "test": "php tests/render-test.php",
    "test:wp": "bash tests/smoke.sh",
    "lint:php": "for f in plugin/accesspath.php plugin/uninstall.php plugin/includes/*.php tests/*.php; do php -l \"$f\" || exit 1; done"
  }
}
